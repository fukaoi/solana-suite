{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "build-configs/tsconfig/base.json",

  "display": "@solana-suite/shared",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "~*": [
        "./src/*"
      ]
      // "*": [
      //   "./src/*"
      // ]
    }
  },
  "include": ["**/*.ts"],
  "exclude": ["node_modules", "dist"]
  // "exclude": ["**/*.test.ts", "node_modules", "dist"]
}
