{
  "name": "@solana-suite/shared",
  "version": "1.0.0",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": true,
  "directories": {
    "version": "1.0.0"
  },
  "files": [
    "dist",
    "changeNetwork",
    "changeDebug",
    "package.json"
  ],
  "scripts": {
    "clean": "rm -rf dist/",
    "compile": "npm run clean && tsc -p tsconfig.json",
    "net:dev": "./changeNetwork dev",
    "net:mainnet": "./changeNetwork mainnet",
    "build": "tsc -b tsconfig.json",
    "test": "mocha -t 300000 --require ts-node/register -- ",
    "test:all": "mocha -t 300000 --require ts-node/register './test/**/*.test.ts'",
    "debug:on": "./changeDebug on",
    "debug:off": "./changeDebug off"
  }
}
