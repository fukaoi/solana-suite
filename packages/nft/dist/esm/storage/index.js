export var Storage;
(function (Storage) {
    class FilePath {
        constructor(filePath) {
            // path.basename(;) 
        }
    }
    Storage.FilePath = FilePath;
})(Storage || (Storage = {}));
